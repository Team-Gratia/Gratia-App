import React from "react";
import styled from "styled-components";

const SmallText = styled.p`
  font-size: 0.875rem;
  line-height: 1.25rem;
  margin-top: 0.25rem;
  color: rgb(107 114 128);
`;

const Agent = () => {
  return <SmallText>We are working on this page 👾...</SmallText>;
};

export default Agent;
